{
  "buildCommand": "cd ../.. && npm install && npm run build --workspace=packages/types && npm run build --workspace=apps/frontend",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
