# Mock Mode Configuration
# Copy this to .env.local or use with: npm run dev:mock
VITE_MOCK_MODE=true

# API Configuration (same port as mock server)
VITE_API_URL=http://localhost:3001/api

# Supabase not needed in mock mode, but keep placeholders to avoid errors
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder-key
